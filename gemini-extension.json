{
  "name": "daily-grind",
  "version": "0.0.1",
  "mcpServers": {
    "nodeServer": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}daily-grind.js"],
      "cwd": "${extensionPath}"
    }
  }
}
